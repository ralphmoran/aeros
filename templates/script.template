<?php

/*
|----------------------------------------------
| "{{worker-name}}" worker
|----------------------------------------------
|
| This worker will be executed in the background by Supervisor.
|
*/

require_once __DIR__ . '/../vendor/autoload.php';

app()
    ->bootstrap()
    ->worker
    ->startWorker(\Aeros\Queues\Workers\{{classname}}Worker::class);

exit(1);
